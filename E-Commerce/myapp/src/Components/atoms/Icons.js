import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

const Icons = ({ icons , className }) => {
  return <FontAwesomeIcon icon={icons} className={className}/>;
};

export default Icons;
